{
  "name": "sprout-expedition-mobile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:web": "node scripts/build-mobile.mjs",
    "sync:android": "npm run build:web && npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "esbuild": "^0.25.0"
  }
}
